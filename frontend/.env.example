# Frontend env — only one variable, set in the Vercel project settings.
# Use the full Render URL, including https:// and no trailing slash.
NEXT_PUBLIC_API_BASE_URL=https://gr-hack.onrender.com

# Local backend alternative:
# NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000
